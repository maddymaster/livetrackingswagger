{
  "name": "comm_server",
  "version": "1.0.0",
  "description": "Communication Server,Listens to various GPS Sending devices and makes a REST API Call to persist GPS Data",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "command-line-args": "^2.1.1",
    "net": "^1.0.2",
    "prototypes": "^1.1.0"
  }
}
